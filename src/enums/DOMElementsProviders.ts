export enum DOMProviders {
  SELECT = "selectDataset",
  IMG_CONTAINER = "imgContainer",
  PAGINATION_CONTAINER = "paginationContainer",
  INFO_CONTAINER = "infoContainer",
  FIGURE_LIST = "figure",
}
